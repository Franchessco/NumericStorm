#include "../../headers/Simplex/SimplexPoint.hpp"
#include "../../headers/Bounds.hpp"

namespace NumericStorm
{
namespace Fitting
{

template<typename T, size_t s>
void SimplexPoint<T, s>::PointToBound(const Bounds<T, s>& other,std::string type)
{
	for (int i = 0; i < s; i++) 
	{
		if (m_parameters )
	}
};

}
}